spring:
  application:
     name: stock-service
  cloud:
     bootstrap:
       enabled: true

eureka:
  client:
     registerWithEureka: true
     fetchRegistry: true
     serviceUrl:
       defaultZone: http://localhost:8086/eureka/
  instance:
     hostname: localhost


 